# Profit Leak Detector (PLD)

A calculator-style business analytics app that identifies hidden profit leaks (discounts, returns, inventory holding) and suggests actionable fixes.

## Run locally
```bash
pip install -r requirements.txt
streamlit run app.py
