# rules.py

THRESHOLDS = {
    "max_discount_pct": 15,       # above this = over-discounting
    "max_return_pct": 5,          # above this = high returns
    "max_stock_days": 45,         # optional if you later compute stock days
    "min_gross_margin_pct": 25    # below this = low margin risk
}
